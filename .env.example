# Email service configuration (EmailJS)
NEXT_PUBLIC_PUBLIC_KEY="your_emailjs_public_key"
NEXT_PUBLIC_SERVICE_ID="your_emailjs_service_id"
NEXT_PUBLIC_TEMPLATE_ID="your_emailjs_template_id"

# GitHub API Token (Optional)
# Provides higher rate limits for GitHub API requests (5000/hour vs 60/hour for unauthenticated)
# Generate a personal access token at: https://github.com/settings/tokens
# Required scopes: public_repo (for public repositories) or repo (for private repositories)
GITHUB_TOKEN="ghp_your_github_personal_access_token"
